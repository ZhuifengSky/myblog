<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.modules.category.dao.ICategoryDao">    
	
	<select id="findList" resultType="com.blog.modules.category.model.Category">
		SELECT id,name FROM category ORDER BY sort
	</select>
</mapper>